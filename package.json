{
  "name": "reachmade-lab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Reachmade Lab — applied AI research, products and implementation. A dependency-free static website.",
  "engines": {"node": ">=22"},
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs && node scripts/serve.mjs",
    "preview": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run build && npm test",
    "media:prepare": "node scripts/prepare-media.mjs",
    "deploy": "npm run check && npm run media:prepare && npx wrangler@4 deploy",
    "deploy:check": "npm run check && npm run media:prepare && npx wrangler@4 deploy --dry-run"
  }
}
